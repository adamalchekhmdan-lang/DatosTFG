# DatosTFG
Aqui voy a guardar los URLs de los datasets que voy a utiliziar en el TFG, de tal manera para que los LLMs puedan acceder al dataset atraves de los URLs ya que en la web no se cargan (estos son datos de prueba iniciales)
